# custom_mean

This directory contains custom mean functions for AEPsych.

Files:

- `custom_basegp_prior_mean.py` — custom prior mean implementation
- `CustomBaseGPPriorMean.ini` — example config

Usage:

- These are designed to be used as extension modules inside the main AEPsych repository.

Notes:

- This README is scaffolded as part of repository modularization work; no source code was changed.
