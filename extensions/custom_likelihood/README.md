# custom_likelihood

This extension contains configurable likelihood implementations used by AEPsych.

Files:

- `custom_configurable_gaussian_likelihood.py` — configurable Gaussian likelihood
- `ConfigurableGaussianLikelihood.ini` — example config

Notes:

- README created as non-invasive scaffolding for splitting into a separate package later.
