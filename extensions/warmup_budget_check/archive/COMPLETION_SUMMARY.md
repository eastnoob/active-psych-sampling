# 独立工作流完成总结

## 已完成的工作

根据您的要求，我已经将两阶段实验规划系统重构为**两个完全独立的脚本**：

### 1. `warmup_sampler.py` - 预热采样生成器

**功能**：
- 接收设计空间CSV（只包含自变量）
- 评估预算充足性
- 交互式询问是否执行采样
- 生成采样文件（subject_1.csv, subject_2.csv, ... 或合并文件）
- 输出实验说明文档

**使用方法**：
```bash
python warmup_sampler.py
```

**输入**：
- 设计空间CSV路径
- 被试数量
- 每个被试的最大承受次数
- 是否跳过交互效应探索

**输出**：
- `sample/subject_N.csv` - 每个被试的采样方案（只有因子列）
- 或 `sample/warmup_samples_all.csv` - 合并的采样方案（包含subject_id列）
- `sample/README.txt` - 实验说明文档

---

### 2. `analyze_phase1.py` - Phase 1数据分析器

**功能**：
- 接收实验数据CSV（包含响应列）
- 交互式询问subject_id和响应列名称
- 分析数据，筛选交互对
- 估计λ参数（交互权重）
- 生成Phase 2配置参数
- 输出详细报告

**使用方法**：
```bash
python analyze_phase1.py
```

**输入**：
- 实验数据CSV路径（包含因变量）
- subject_id列名（默认: "subject_id"）
- 响应列名（默认: "response"）
- Phase 2被试数和trials数

**输出**：
- `phase1_analysis_output/phase1_phase2_config.json` - JSON格式配置
- `phase1_analysis_output/phase1_phase2_schedules.npz` - NumPy格式动态调度
- `phase1_analysis_output/phase1_analysis_report.txt` - 人类可读报告
- `phase1_analysis_output/PHASE2_USAGE_GUIDE.txt` - 使用指南

---

## 工作流程

### 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: 生成预热采样方案                                      │
├─────────────────────────────────────────────────────────────┤
│ 输入: design_space.csv (只有自变量)                           │
│       ├─ density, height, greenery, ...                     │
│       └─ 1200行配置                                         │
│                                                             │
│ 执行: python warmup_sampler.py                              │
│       ├─ 评估预算充足性                                      │
│       ├─ 询问确认                                           │
│       └─ 生成采样文件                                        │
│                                                             │
│ 输出: sample/subject_N.csv (只有因子列)                       │
│       ├─ subject_1.csv                                      │
│       ├─ subject_2.csv                                      │
│       ├─ ...                                                │
│       └─ README.txt                                         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 中间步骤: 执行实验（用户自行完成）                              │
├─────────────────────────────────────────────────────────────┤
│ 1. 按照subject_N.csv文件执行实验                              │
│ 2. 记录每个配置的响应值                                       │
│ 3. 将响应值添加到CSV中                                        │
│                                                             │
│ 结果: warmup_data.csv (添加了响应列)                          │
│       subject_id, density, height, ..., response            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: 分析数据并生成Phase 2参数                             │
├─────────────────────────────────────────────────────────────┤
│ 输入: warmup_data.csv (包含因变量)                            │
│                                                             │
│ 执行: python analyze_phase1.py                              │
│       ├─ 加载实验数据                                        │
│       ├─ 筛选交互对                                          │
│       ├─ 估计λ参数                                           │
│       └─ 生成Phase 2配置                                     │
│                                                             │
│ 输出: phase1_analysis_output/                               │
│       ├─ phase1_phase2_config.json                          │
│       ├─ phase1_phase2_schedules.npz                        │
│       ├─ phase1_analysis_report.txt                         │
│       └─ PHASE2_USAGE_GUIDE.txt                             │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Phase 2: EUR-ANOVA主动学习                                   │
├─────────────────────────────────────────────────────────────┤
│ 使用Phase 1输出的参数:                                        │
│ - interaction_pairs: [(0,2), (1,5), ...]                   │
│ - lambda_init: 0.950                                       │
│ - gamma_init: 0.300                                        │
│ - lambda/gamma动态调度                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 创建的文件列表

### 核心脚本

1. **`warmup_sampler.py`** (376行)
   - 独立的预热采样生成器
   - 包含预算评估和采样文件生成

2. **`analyze_phase1.py`** (460行)
   - 独立的Phase 1数据分析器
   - 包含交互对筛选和Phase 2参数生成

### 文档

3. **`README_STANDALONE.md`**
   - 完整的独立工作流使用指南
   - 包含所有步骤的详细说明
   - FAQ和参数说明

4. **`COMPLETION_SUMMARY.md`** (本文件)
   - 工作完成总结
   - 文件列表和使用说明

### 测试脚本

5. **`test_standalone_workflow.py`** (280行)
   - 非交互式演示脚本
   - 展示完整工作流
   - 可用于验证功能

---

## 与原有代码的关系

### 保留但未修改的文件

- **`eur_anova_pair.py`** - EUR-ANOVA采集函数（**未修改**，仅用于理解参数需求）
- **`warmup_budget_estimator.py`** - 预算评估工具（被warmup_sampler.py使用）
- **`phase1_analyzer.py`** - 核心分析函数（被analyze_phase1.py使用）

### 新增的独立脚本

- `warmup_sampler.py` - 封装了预算评估 + 采样生成
- `analyze_phase1.py` - 封装了数据分析 + Phase 2参数生成

---

## 关键特性

### ✓ 完全独立
- 两个脚本完全独立运行
- 中间由用户执行实际实验
- 无需担心状态传递

### ✓ 交互式友好
- 清晰的提示和默认值
- 预算评估后询问确认
- 输出人类可读的报告

### ✓ 参数对齐
- Phase 2参数完全对齐`eur_anova_pair.py`
- 输出`interaction_pairs`, `lambda_init`, `gamma_init`
- 提供完整的λ/γ动态调度表

### ✓ 灵活配置
- 支持合并/分文件两种模式
- 可自定义列名
- 交互对筛选方法可选（elbow/bic_threshold/top_k）

---

## 使用示例

### 示例1: 分文件模式

```bash
# Step 1: 生成采样方案
$ python warmup_sampler.py
请输入设计空间CSV路径: design_space.csv
被试数量: 14
每个被试的最大承受次数: 25
是否跳过交互效应探索？(y/N): N

[评估] 充足性: 【刚好】
是否生成采样方案？(Y/n): Y

输出目录: sample
是否合并为单个CSV？(y/N): N

[OK] 已生成14个被试文件

# 用户执行实验，收集数据...

# Step 2: 分析数据
$ python analyze_phase1.py
请输入实验数据CSV路径: warmup_data.csv
被试编号列名: subject_id
响应变量列名: response

[分析] 筛选出的交互对: 3个
  1. (density, greenery): score=142.460
  2. (height, style): score=48.072
  3. (density, style): score=0.140

λ估计: 0.897

Phase 2被试数量: 18
每个被试的测试次数: 25
λ调整系数: 1.2

[OK] 分析完成！
```

### 示例2: 合并文件模式

```bash
# Step 1: 生成合并的采样文件
$ python warmup_sampler.py
...
是否合并为单个CSV？(y/N): y
被试编号列名: subject_id

[OK] 已生成合并文件: sample/warmup_samples_all.csv

# 用户执行实验后...

# Step 2: 分析（使用相同的subject_id列名）
$ python analyze_phase1.py
...
被试编号列名: subject_id
...
```

---

## Phase 2集成示例

```python
# 加载Phase 1输出的配置
import json
import numpy as np

with open('phase1_analysis_output/phase1_phase2_config.json') as f:
    config = json.load(f)

data = np.load('phase1_analysis_output/phase1_phase2_schedules.npz')
lambda_schedule = data['lambda_schedule']
gamma_schedule = data['gamma_schedule']

# 初始化EUR-ANOVA
from eur_anova_pair import EURAnovaPairAcqf

acqf = EURAnovaPairAcqf(
    model=your_gp_model,
    gamma=config['gamma_init'],  # 0.300
    lambda_min=0.1,
    lambda_max=1.0,
    interaction_pairs=config['interaction_pairs'],  # [(0, 2), (1, 5), (0, 5)]
    tau1=0.7,
    tau2=0.3,
    tau_n_max=config['total_budget'],  # 450
    gamma_min=config['gamma_end']  # 0.060
)

# 主动学习循环
for trial in range(1, config['total_budget'] + 1):
    lambda_t = lambda_schedule[trial - 1, 1]
    gamma_t = gamma_schedule[trial - 1, 1]

    # EUR-ANOVA采集
    scores = acqf(X_candidates)
    X_next = X_candidates[scores.argmax()]

    # 执行实验
    y_next = conduct_experiment(X_next)

    # 更新模型
    your_gp_model.update(X_next, y_next)

    # 中期诊断
    if trial == config['mid_diagnostic_trial']:
        run_diagnostic()
```

---

## 测试

运行测试脚本验证功能：

```bash
python test_standalone_workflow.py
```

这将：
1. 创建示例设计空间
2. 生成预热采样方案
3. 模拟执行实验
4. 分析数据
5. 生成Phase 2配置
6. 验证所有输出文件

---

## 注意事项

1. **未修改`eur_anova_pair.py`**
   - 按您的要求，只参考未修改
   - 输出参数完全对齐其接口

2. **列名灵活性**
   - subject_id和response列名可自定义
   - 因子列名自动识别

3. **交互对索引**
   - 基于因子列的顺序（0-indexed）
   - 确保Phase 2使用相同的因子顺序

4. **动态调度**
   - 使用指数衰减策略
   - 可在`analyze_phase1.py`中修改衰减函数

---

## 下一步

1. **测试工作流**
   ```bash
   python test_standalone_workflow.py
   ```

2. **实际使用**
   - 准备您的设计空间CSV
   - 运行`warmup_sampler.py`生成采样方案
   - 执行实验收集数据
   - 运行`analyze_phase1.py`获取Phase 2参数

3. **查阅文档**
   - `README_STANDALONE.md` - 完整使用指南
   - 生成的`PHASE2_USAGE_GUIDE.txt` - Phase 2集成说明

---

## 总结

✅ **已完成**：
- 两个完全独立的脚本（warmup_sampler.py + analyze_phase1.py）
- 完整的使用文档（README_STANDALONE.md）
- 测试脚本（test_standalone_workflow.py）
- Phase 2参数完全对齐EUR-ANOVA接口
- 未修改`eur_anova_pair.py`

✅ **满足需求**：
- "两个独立的步骤"
- "首先我给你喂全组合的csv（只有自变量没有因变量）"
- "你判断这个够不够，然后问我是不是要执行采样"
- "我选择是的话，你给我采样结果"
- "然后我自己做实验，这个过程你不参与"
- "所有预热被试的实验完成之后，我把做好的数据（可能也是csv）喂给你"
- "以报告的形式给我可行的参数"

🎯 **可以直接使用！**
