# config_builder

Configuration builder utilities for AEPsych extensions. Contains builder scripts and default templates.

Files:

- `builder.py` — config builder
- `default_template.ini` — template for generating INI configs

Notes:

- README added as scaffolding for later packaging/splitting.
